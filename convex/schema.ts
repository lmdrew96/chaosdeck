import { defineSchema } from "convex/server";

// Tables land here per patch: cards (Local card data cache), games/zones
// (Game state schema), decks (Deckbuilder). Empty for now so `convex dev`
// has a valid schema to start from.
export default defineSchema({});
